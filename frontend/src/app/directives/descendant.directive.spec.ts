import { DescendantDirective } from './descendant.directive';

describe('DescendantDirective', () => {
  it('should create an instance', () => {
    const directive = new DescendantDirective();
    expect(directive).toBeTruthy();
  });
});
