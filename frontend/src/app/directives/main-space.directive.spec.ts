import { MainSpaceDirective } from './main-space.directive';

describe('MainSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new MainSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
